{
  "name": "plane-e2e",
  "private": true,
  "description": "End-to-end tests against a locally running Plane API. Standalone on purpose: e2e/ sits outside the pnpm workspace globs, so this cannot disturb the monorepo's dependency tree.",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
